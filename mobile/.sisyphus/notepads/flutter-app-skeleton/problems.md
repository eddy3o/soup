(Blockers and technical debt will be tracked here)
- Product repository methods are intentionally unimplemented and return `UnimplementedFailure("TODO")` until the API layer is wired up.
- Category repository impl is intentionally a TODO stub that returns `UnimplementedFailure("TODO")` until GET /categories is wired up.
